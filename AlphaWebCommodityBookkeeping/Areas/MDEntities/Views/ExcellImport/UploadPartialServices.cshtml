@using (Html.BeginForm("Callbacks", "ExcellImport", FormMethod.Post, new { @class = "imagePreview" }))
{
    <div class="uploadContainer">
        <label>Odaberite datoteku:</label>
        @Html.DevExpress().UploadControl(
            settings => {
                settings.Name = "ucCallbacksServices";
                settings.CallbackRouteValues = new { Controller = "ExcellImport", Action = "CallbacksImageUploadServices" };
                //settings.ControlStyle.CssClass = "editorArea";
                settings.BrowseButton.Text = "Odaberi";
                settings.ShowProgressPanel = false;
                //settings.ValidationSettings.Assign(UploadControlDemosHelper.ValidationSettings);

                //settings.ClientSideEvents.FileUploadComplete = "function(s,e){ if(e.callbackData != '') $('#previewImage').attr('src', e.callbackData); }";
                settings.ClientSideEvents.FileUploadComplete = "function(s,e){ if(e.callbackData != '') {ImportServices(e.callbackData); $('#Uploaded').fadeIn('slow')}}";
            }).GetHtml()
        <label>&nbsp;</label>
        <div class="editorArea text">
            
        </div>
        <label>&nbsp;</label>
        <div class="editorArea">
            <input type="button" value="Pošalji" onclick="ucCallbacksServices.Upload(); $('#Uploaded').hide();" />
        </div>
    </div>
   @* <div class="imageContainer">
        <img alt="" id="previewImage" src="@Url.Content("~/Content/ImagePreview.gif")" />
    </div>*@
    
}